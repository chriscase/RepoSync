# RepoSync PR #72 — correction-first continuation and SVN-apply safety

## Assignment

Continue `chriscase/RepoSync`, branch `feature/reposync-reliability`, draft PR #72, from reviewed head:

```text
d7d4ce30af3b0abb2971bbc106bd469631196bf9
```

Read [review 2](https://github.com/chriscase/RepoSync/pull/72#pullrequestreview-5298436452) and the companion `RepoSync-PR72-review-2.md`. This is a bounded continuation of #61/#62/#63/#66, not authorization to implement the entire epic.

**Outcome:** legitimate alternating SVN/Git activity must not be falsely blocked; rejected or admitted operations must not silently destroy local/unchanged content; failed SVN apply must not acknowledge unapplied work. Evidence publication must fail closed when required verification cannot execute.

Use two stages with separate commits. Complete the correction controls in Stage A before beginning Stage B. No planning-only review is needed between stages when the required controls pass. Stop for independent review after this bounded pass.

## Identity and contract

Original base: `87379741779a6259f7eeb52a68cc6f061174e5ef`.
Previous gate functional head: `6679f84202be59c810753db714494e2b59a66a15`.
Original GOAL SHA-256: `16003181005349892c486d92ac980951c7cb564ab6d45742588df581955eaec8`.
Prior continuation SHA-256: `266cb1eea867e65b53483c25107b55556e915a3d18e746e7bdbf85d704e6e703`.
Reviewed dependency graph SHA-256: `9feb01d8964bc93d67ff94014fcd1c7ff0f86111dc5e0efceaf288ec12cc2d05`.

Keep original GOAL and previous brief bytes unchanged. Copy this new brief into a separately named review/continuation file, record its hash, and identify it in the next handoff. Do not rewrite acceptance criteria, close issues automatically, or relabel partial scenario coverage as complete.

Inspect branch/worktree before editing. Continue only this work; do not reset, stash or discard unrelated changes. Record any head movement. Use ordinary commits and pushes, not rebase/force-push. Keep PR72 draft. No merge, deployment, release, production endpoints/credentials, active repository operations, checkpoint reset, schema migration, general job service, automatic rebase, or live acceptance.

## Stage A — close the gate and evidence defects

### G01: healthy directional checkpoint transitions

Trace every affected per-repo Git checkpoint reader/writer, including full import, SVN→Git, Git→SVN and legacy fallbacks. `team_git_checkpoint()` currently treats any repository-column/KV disagreement as ambiguity, but successful SVN→Git advances only the column. A state generated by RepoSync itself must not automatically be classified as corruption.

Implement the minimum justified compatibility correction without a new schema. Explicitly distinguish authoritative, demonstrably stale, absent and genuinely conflicting state. Keep repo ownership, full object IDs and mapping evidence. Do not choose a cursor solely from observed HEAD, hash ordering, final-tree equality, arbitrary global fallback or a convenient tip. Do not erase conflicting data just to make validation pass.

Fix writer/reader coherence while preserving separate directional semantics. A newly emitted SVN→Git commit must not cause pending Git-origin work to be skipped. Required persistence failure must not be converted into success. If proof for a legacy case is insufficient, preserve evidence and report the affected pair as unresolved; do not blanket-block every healthy installation or silently admit every mismatch.

Add actual-engine tests exercising persisted states generated by normal paths, not only constructor-seeded rows with missing KV:

- SVN-derived baseline → successful Git→SVN that populates both copies → SVN-only change → successful SVN→Git → unchanged poll → reopened engine → further Git work.
- Equivalent qualified legacy state with column/KV divergence produced by the pinned old code or a precisely explained historical fixture.
- Missing KV remains supported where proven; conflicting/unrelated cursor remains rejected; other repositories' state remains untouched.
- Pending Git work concurrent with an incoming SVN change cannot be skipped by a publication-induced cursor update.

Assert exact source/target revisions, mappings and cursors throughout. Preserve the prior P/O/R/L distinctions. Use a supported deterministic path shape for the minimal gate regression; do not hide unrelated old apply defects as success.

### G02: protect ignored collisions and make inspection read-only

The supplied reviewer probe is Git-only evidence. Turn its cases into production-gate/actual-engine tests within the container.

Before reset, detect local files/directories that would be overwritten by the selected target, including ignored paths. Preserve them or return a specific safe block before destructive effects. Do not delete the collision to proceed. A harmless noncolliding ignored cache should not unnecessarily block a healthy sync. Handle file↔directory collisions and do not follow symlinks outside owned paths.

Use read-only inspection where the contract promises unchanged index/worktree on rejection. `git --no-optional-locks status` is a relevant mechanism; audit other probes too. Record raw index and path evidence before any helper can refresh or mutate it. Touch a tracked file's mtime without changing bytes before a rewrite rejection, and verify the exact pre-engine index is preserved. Do not make the test pass by refreshing first, excluding the index from the manifest or redefining preservation.

Retain independent developer/remote/bridge clones, metadata-amend rejection, dirty/staged/unpublished local tests, repeat/restart rejection, legitimate fast-forward and L=R/P-lagging controls. The protection must work in normal runtime code, not only behind a test-only branch.

### G03: required evidence scan must execute and fail closed

Fix `scripts/reliability-container.sh`'s unchecked `rg` condition. Either use an existing guaranteed scanner such as Python or explicitly require and verify the tool. Distinguish no match from scanner failure. Bound and confine scans to the intended evidence directory; use safe handling for unreadable files and symlinks.

Test:
- clean fixture evidence → explicit verified scan success;
- synthetic canary present → nonzero result, no success-labelled publication;
- scanner missing/failed/read error → nonzero result, not clean;
- failure artifacts are not uploaded raw under a sanitized label.

Do not log the canary value unnecessarily. Include required scan execution/result in the summary. The reviewer found no exact canary in the previous downloaded artifact, but CI did not run its scan; preserve that historical distinction.

## Stage B — F06 and G04: truthful, content-preserving SVN application

Begin only after Stage A controls pass. Keep the following runtime fixes separately reviewable from harness/checkpoint corrections.

### F06: no checkpoint over a failed revision

A failed non-empty SVN apply is an error, not a filtering decision. Stop before processing later dependent revisions or outgoing work that would rely on the failed incoming state. Do not merely remove `advance_svn_watermark` and keep `continue`: a later revision could still advance beyond the missing work.

Preserve earlier verified outcomes; leave failed N and later N+1 pending. No new successful mapping, success counter or completed outcome for unapplied work. Do not reset the checkpoint, fall back to full import/export overwrite, or change the remote history.

Use the real engine and real disposable SVN/Git repositories. Deterministic subprocess failure injection is permitted if clearly identified and it reaches the production apply/error path rather than failing admission earlier. Prove successful N-1, failed N, queued N+1, an accurate error, stable durable state at the last justified frontier, and retry of N/N+1 exactly once after the pre-commit failure is corrected. A later unchanged cycle must be a no-op. Verify intermediate as well as final trees/mappings.

Keep an intentional metadata-only/filtered revision distinct from apply failure, with explicit evidence for any no-target checkpoint. Do not turn every empty commit or patch error into success. Uncertain post-push response/DB failures are still #64 recovery gaps; preserve and report them instead of claiming an atomic rollback.

### G04: no deletion based on missing membership in a change list

Remove/replace the top-level cleanup heuristic that treats the current revision's changed paths plus `source`/`config` as the complete keep-list. A delta is not a full tree. Do not simply expand the hard-coded directory list.

Apply deletion only when justified by the SVN operation and active branch/path policy. Preserve untouched tracked content and local state. Do not auto-repair legacy wrong-path history or remove existing paths whose ownership is uncertain. Keep full-history import, path-scope and LFS contracts unchanged; do not implement unrelated monorepo work in this pass.

Actual-engine controls must include untouched top-level files and directories outside `source`/`config`, an added path, an ordinary modification, and an explicit SVN deletion. Assert a canonical manifest of the entire expected projected tree (including untouched bytes), exact emitted Git changes, mapping/checkpoints and a no-op repeated cycle. A new file existing or mapping count increasing is insufficient.

## Required-case additions

Extend, never renumber, the existing manifest and R01–R24 scenario definitions. Suggested stable subcase IDs below may use repository naming conventions, but the final manifest must enumerate every required case exactly and fail if it disappears, is ignored, filtered out, or unsuccessful.

| Suggested ID | Required proof |
|---|---|
| R01_ALTERNATING_DUAL_CURSOR | Production-generated dual checkpoint, both directions, repeat/reopen and further work |
| R10_LEGACY_STALE_COPY | Evidence-backed legacy cursor compatibility; genuine conflict still blocked |
| R01_PENDING_BOTH_DIRECTIONS | Incoming SVN publication does not acknowledge pending Git-origin work |
| R09_IGNORED_FILE_COLLISION | Ignored local bytes preserved when R introduces same tracked path |
| R09_IGNORED_DIRECTORY_COLLISION | File/directory collision protected |
| R09_READONLY_INDEX | Fresh stat-cache mismatch; rejection preserves exact original index |
| R01_IGNORED_NONCOLLISION | Harmless ignored state preserved without blanket rejection |
| EVIDENCE_SCAN_CLEAN | Scanner actually executes and accepts clean evidence |
| EVIDENCE_SCAN_CANARY | Canary presence fails |
| EVIDENCE_SCAN_ERROR | Missing/failed scanner or read failure cannot pass |
| R01_FAILED_APPLY_BARRIER | N-1 success, N fail, N+1 pending; truthful progress and no skipped frontier |
| R01_FAILED_APPLY_RETRY | Safe retry applies missing work once; unchanged cycle does nothing |
| R19_UNTOUCHED_TREE | Successful delta retains unrelated top-level files/directories |
| R19_EXPLICIT_DELETE | Genuine SVN delete still propagates; untouched paths survive |

Retain all 21 prior exact cases unless an explicitly reviewed correction changes a baseline defect observation into a candidate assertion. Baseline observations remain distinct. Auth/fetch error injections are not real enterprise authentication tests. Admission-only tests cannot satisfy content-synchronization acceptance.

Run the same isolated exact suite, scanner self-tests and matched-lock comparison against the original base. Preserve test identity tracking and disclose each changed old test/oracle. Do not suppress the five known base failures or the ignored conflict case. If a targeted safety fix resolves a known failure, provide cause and content evidence. Broad formatting/lint cleanup and unrelated runtime fixes remain out of scope.

## Handoff and stopping condition

Update `docs/reliability/review/latest.md` and PR72. Link this review and related issues with `Refs`, not automatic closing language. Publish normal commits, current CI links and sanitized downloadable artifacts. Preserve small durable result manifests in Git when necessary.

Return:

```text
STATUS: READY_FOR_REVIEW | BLOCKED
PR / BRANCH:
ORIGINAL BASE / PREVIOUS REVIEWED HEAD:
HARNESS/CHECKPOINT CORRECTION HEAD:
SVN-APPLY FUNCTIONAL HEAD:
FINAL DOCUMENTATION HEAD / CI MERGE REF / TREE:
REMOTE MATCH / WORKTREE:
ORIGINAL GOAL HASH / THIS CONTINUATION HASH / LOCK HASH:

FINDING DISPOSITIONS:
F01–F06 and G01–G04, each PASS / FAIL / PARTIAL / NOT RUN,
with code location, exact test IDs, evidence tier and remaining limit.

EVIDENCE:
Commands, exit codes, exact required-case outcomes and scanner execution.
Full before/after tree, index, cursor and mapping proofs.
Matched-base comparison, retained failures/ignores and changed assertions.
CI artifact link, expiry, API digest and independently recomputed digest
clearly distinguished.

OPEN:
Unfulfilled acceptance and actual-deployed-version coverage.
Any new finding that prevents the bounded proof; no hidden workaround.

NEXT DECISION:
One bounded review/continuation request.
```

Do not declare the whole gate/epic complete from subcase passes. Do not claim the active installation is compatible without its qualification. If additional failures require more scope, stop with the actual code/evidence and proposed minimum next change, not a weaker oracle. After this pass, wait for Chris to relay the handoff for independent review. No background agent-to-reviewer channel is assumed.
