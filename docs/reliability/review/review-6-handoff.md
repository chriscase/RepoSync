# PR #72 J01–J03 and #54/#63 proposal: bounded review handoff

**Status:** draft, awaiting independent review. Original base `87379741779a6259f7eeb52a68cc6f061174e5ef`; immediate reviewed start `649dad8272dedbe16592c183ba37066c01965bac`; retained matched comparison anchor `f74fce855a1f1d80dd631397f436ba33906272e6`. The original GOAL SHA-256 remains `16003181005349892c486d92ac980951c7cb564ab6d45742588df581955eaec8`; Cargo.lock SHA-256 remains `9feb01d8964bc93d67ff94014fcd1c7ff0f86111dc5e0efceaf288ec12cc2d05`. The copied current brief SHA-256 is `67e93e3133503b68467bd5ff0f3decba195970bc2e959eef952fcfedc17abcab`. Prior handoff bytes are preserved as [review-5-handoff.md](review-5-handoff.md).

Functional changes end at `a98b394cd9b265b4eec59e52ac3d9faac1b02a77`. Commits are separate: `c76f505` J01 semantics, `fd434a2` J02/J03 inventory and probes, `4dfe735` no-target symlink read refusal, `87525a7` documentation-only migration proposal, `a98b394` parallel-test pause isolation. The final documentation head and CI artifact identities are recorded on the draft PR and in the return handoff.

## J01 semantic no-target proof

The existing `R01_VERIFIED_NO_DELTA` identity remains, but its old mode-only, byte-equality success oracle is explicitly corrected: Git mode 100755 is refused before an SVN write, receipt, mapping or cursor advance. Its queued successor remains pending. `R01_REPRESENTED_CONTENT` creates a genuine regular-file content delta and uses a controlled, independently committed SVN revision to make the intended bytes present. It verifies a version-3 receipt for `regular_file_bytes_no_properties_v1`, pinned SVN UUID/URL/revision, Git mode 100644, regular SVN file, byte hash and no SVN file properties. It also demonstrates refusal of a restored old version-2 byte-only receipt. `R01_TARGET_MISMATCH` changes the pinned SVN target again before verification and proves neither the offending commit nor its successor is acknowledged. The verifier checks symlinked export ancestors before reading them. These local observations do not establish a general multiwriter publication contract.

## J02 inventory authority and J03 sealed reads

The command enumerates repository columns, scoped SVN/Git KV, legacy global SVN/Git KV, import watermarks and progress, scoped applied rows, legacy global mapping references, schema columns/indexes/FKs, no-target receipt proof levels, and credential presence/parent/global references. It reports disagreements separately rather than choosing a maximum. The pinned original-code driver creates two independent SVN-origin imports with overlapping r2 and a disabled third repository; 14 labeled overlays vary each source and unsafe path shape on copies. The combined overlay exposes repository SVN=2, scoped SVN=999, global SVN=888 and import watermark=777 as conflicting sources requiring reconciliation. The report never upgrades local fixture shape to remote UUID/copy/ref qualification.

Every dynamic Git ref is validated before path construction and read through sealed, no-follow file descriptors. Packed refs have a bounded reader; linked worktrees are unqualified. The probe audit hook checks attempted opens and finds zero external-canary opens for absolute/traversal refs, invalid ID and symlink-ancestor cases. The reports remain sanitized; private seals and secret values are not published. The command still requires a quiesced consistent copy and does not inspect an active installation.

## Migration boundary and remaining proof

[Migration proposal](../migration-proposal.md) contains candidate v13 #54 nullable `commit_map.git_sha` rebuild and v14 `(repo_id,generation)` lineages, handled/emitted frontiers and typed outcomes, with constraints and fixture-by-fixture conversion dispositions. It uses the actual embedded migration runner location, defines atomic version updates, restart/refusal and preservation checks, and separates pre-write restoration from later #64 post-effect reconciliation. This PR executes **no DDL**. Deployed executable/schema, remote SVN UUID/copy ancestry, remote Git identity, historical filtered decisions, already-pruned proof and other legacy shapes remain **NOT ESTABLISHED**. The smallest next implementation slice is a separately reviewed transactional #54 nullable mapping plus #63 generation-owner migration on qualified copies, with old-row preservation and interruption tests; a general #64 operation service remains later work.

The strict manifest now requires 51 cases: all 48 previously required identities remain, the mode-only oracle is the one authorized strengthening, and three new exact cases cover represented content, target mismatch and inventory confinement. Known diagnostic failures and ignored controls remain visible. The local parallel team E2E run after pause isolation completed 52 passed, 0 failed, 1 ignored. Final strict-case counts, matched comparisons, scanner result and downloadable CI artifact hashes are supplied in the PR handoff after CI completion. No production access, checkpoint reset, reimport recovery, schema execution, merge, deployment or force push was performed.
